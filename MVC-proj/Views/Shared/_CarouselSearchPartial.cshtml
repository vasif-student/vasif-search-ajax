@model List<Carousel>

@if (Model.Count == 0)
{
    <li>Netice tapilmadi</li>
}
else
{
    @foreach (var item in Model)
    {
        <li>@item.ImageName<img src="~/img/@item.ImageName"/></li>
    }
}